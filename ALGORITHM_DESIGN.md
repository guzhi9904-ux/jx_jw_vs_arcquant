# ARCQuant / NVFP4 算法设计记录

这份文档用来持续记录本仓库中的量化算法、实验性变体和硬件落地方案。

写作原则：

- 先用大白话说明“为什么做、到底做了什么”，再给公式。
- 明确区分离线校准和在线推理，避免把校准统计误认为推理开销。
- 每个算法使用独立版本号，旧版本不覆盖，方便追踪思路变化。
- 诊断用方法和准备落地的方法必须分开标注。
- 记录数值收益时，同时记录校准预算、量化后端和硬件限制。

---

## 1. 版本总表

| 版本 | 名称 | 核心想法 | 当前定位 | 状态 |
|---|---|---|---|---|
| V0 | 普通 W4A4 RTN | 激活和权重直接 NVFP4 Round-to-Nearest | 所有方法的基础基线 | 已实现 |
| V1 | 论文 reorder + ARC | 根据激活重要性重排，并增加激活残差补偿列 | 论文复现基线 | 已复现 |
| V2 | 逐通道联合 K+S，固定 1:1 | 分别选择激活残差通道和权重残差通道，各使用一半预算 | 当前主要算法候选 | 已完成数值验证 |
| V2.1 | 逐通道联合 K+S，自适应分配 | 根据两类统计量自动决定激活/权重分支各占多少 S | 机制诊断，不作为最终方案 | 已完成数值验证 |
| V3 | 16-block 共享 K+S | 以完整 16-channel block 选择，并在 QKV、Gate/Up 之间共享 | 硬件约束下界实验 | 已完成数值验证，暂不替代 V2 |

当前建议：

- 算法研究主线继续以 **V2 逐通道联合 K+S** 为中心。
- V3 证明了严格整块选择会损失较多精度，它是硬件约束实验，不代表逐通道一定无法落地。
- 下一步应优先验证 V2 的融合抽取与单次 K+S GEMM，而不是立即放弃逐通道选择。

---

## 2. 公共符号和概念

### 2.1 一个线性层在做什么

一个线性层可以写成：

\[
Y=XW^T
\]

其中：

| 符号 | 大白话含义 | 典型形状 |
|---|---|---|
| \(X\) | 当前线性层收到的激活 | \([M,K]\) |
| \(W\) | 原始浮点权重 | \([N,K]\) |
| \(Y\) | 线性层输出 | \([M,N]\) |
| \(M\) | 这次一起计算多少个 token；decode 通常为 1，prefill 通常较大 | — |
| \(K\) | 输入通道数，也就是输入 hidden dimension | — |
| \(N\) | 输出通道数 | — |

### 2.2 什么是通道

假设输入有 8 个数：

```text
X = [x0, x1, x2, x3, x4, x5, x6, x7]
```

这里的第 0 到第 7 个位置就是 8 个输入通道。

真实模型中，如果一个线性层的输入宽度为 1536，那么通道编号就是：

```text
0, 1, 2, ..., 1535
```

线性层的第 \(j\) 个输入通道，对应权重矩阵 \(W\) 的第 \(j\) 列。

### 2.3 什么是索引

索引只是一组整数编号，用来告诉程序“取哪些位置”。

例如：

```text
indices = [17, 203, 816, 1201]
```

意思是取输入的第 17、203、816 和 1201 个通道。

索引不是激活值，不是权重值，也不是 token 位置或 KV-cache 位置。

### 2.4 什么是量化残差

激活量化：

\[
QX=\operatorname{Quant}(X)
\]

激活残差：

\[
E_X=X-QX
\]

权重量化：

\[
QW=\operatorname{Quant}(W)
\]

权重残差：

\[
E_W=W-QW
\]

残差就是量化时丢掉的那部分信息。

---

## 3. V0：普通 W4A4 RTN

### 3.1 大白话

普通 RTN 把激活和权重都直接量化成 NVFP4，然后做低精度矩阵乘法：

\[
Y_{\text{RTN}}=QXQW^T
\]

它不额外选择通道，也不做补偿。

### 3.2 主要问题

RTN 的误差不只有“普通通道被 outlier 挤成 0”。当前实验观察到：

- 零化项合计贡献约 18.76%。
- 没有归零、但被粗量化网格推偏的项合计贡献约 81.24%。
- 从激活和权重两侧看，贡献约为 47.33% / 52.67%。

因此，只保护激活大值不能覆盖全部问题。

---

## 4. V1：论文 reorder + ARC

### 4.1 大白话

论文方法主要根据激活重要性找到需要额外保护的输入通道，把这些通道重排到适合 ARC 处理的位置，再增加一组激活残差补偿列。

它的重点是补偿：

\[
(X-QX)W^T
\]

### 4.2 当前定位

V1 是论文复现对照，不是本文档当前要扩展的主要算法。

已经观察到：

- 任意通道重排可能改变 NVFP4 原始 16-channel 分组。
- 分组变化后，outlier 和普通值可能进入新的量化组，改变 NVFP4 原本的保护效果。
- 论文 ARC 仍能改善最终输出，但它没有独立设计权重残差分支。

---

## 5. V2：逐通道联合 K+S，固定 1:1

### 5.1 一句话说明

给每个普通 RTN 线性层增加一个“小型纠错外挂”：

- 主 \(K\) 维仍然全部执行普通 NVFP4 RTN。
- 离线找出少量最值得照顾的激活误差通道和权重误差通道。
- 在线增加宽度为 \(S\) 的补偿计算，把这些通道丢失的信息加回输出。

### 5.2 动机：RTN 有两条误差路径

RTN 相对浮点输出的误差可以精确拆成：

\[
XW^T-QXQW^T
=
(X-QX)W^T+QX(W-QW)^T
\]

大白话：

1. 激活被量化错了，会产生一部分输出误差。
2. 权重也被量化错了，会产生另一部分输出误差。

这两种误差的重要通道不一定相同，所以需要分别选择。

### 5.3 \(J_X\) 和 \(J_W\) 是什么

\(J_X\) 是“激活残差补偿通道编号列表”。

例如：

```text
J_X = [17, 203, 816, 1201]
```

意思是这一层重点补偿第 17、203、816、1201 个通道的激活量化误差。

\(J_W\) 是“权重残差补偿通道编号列表”。

例如：

```text
J_W = [9, 203, 742, 1305]
```

意思是这一层重点补偿这些通道对应的权重量化误差。

注意：

- 每个线性层都有自己的 \(J_X,J_W\)。
- \(J_X\) 和 \(J_W\) 可以不同。
- 同一个通道可以同时出现在两份列表中，因为它可能同时有较大的激活误差和权重误差。
- 同一个通道如果在两份列表中出现，会占用两个补偿列，因为两列负责不同的误差项。

### 5.4 什么叫“离线固定的 \(J_X,J_W\)”

固定的是通道位置，不是激活数值。

可以类比为：

- \(J_X,J_W\) 是提前写好的座位号。
- 每次新 prompt 进入模型，坐在这些位置上的激活值都会变化。
- 但程序每次都检查同样的座位。

只要模型、量化设置和校准结果不变，部署后不会重新做通道打分或 top-k。

---

## 6. V2 的离线流程

离线部分发生在模型部署之前。

### 离线步骤 1：量化权重

计算：

\[
QW=\operatorname{Quant}(W)
\]

\[
E_W=W-QW
\]

\(E_W\) 是权重量化丢掉的信息。

### 离线步骤 2：用校准文本运行模型

准备一些校准文本，正常运行模型，记录每个线性层收到的激活 \(X\)。

当前 V2 数值实验中，每个 seed 使用：

- 4 条 2048-token 选择样本。
- 每个模块从这些样本中固定抽取 512 个 token row 计算统计量。
- 另有 4 条互斥 holdout 样本，只用于评估，不参与选通道。

### 离线步骤 3：模拟部署时的激活量化

对校准激活执行与部署相同的量化：

\[
QX=\operatorname{Quant}(X)
\]

\[
E_X=X-QX
\]

这里的 \(E_X\) 只在离线校准中用于统计通道重要性。

### 离线步骤 4：给每个通道打两个分数

激活残差分数：

\[
s_j^X
=
\sum_t E_X[t,j]^2
\cdot
\sum_n W[n,j]^2
\]

大白话：

> 如果这个通道的激活量化误差经常很大，而且它对应的权重列能强烈影响输出，那么它值得进行激活残差补偿。

权重残差分数：

\[
s_j^W
=
\sum_t QX[t,j]^2
\cdot
\sum_n E_W[n,j]^2
\]

大白话：

> 如果这个通道在校准数据中经常有较大的量化激活，而且它对应的权重量化误差很大，那么它值得进行权重残差补偿。

这里使用的是简单二阶统计量，不读取 output residual，不执行逐候选 GEMM，也不使用迭代贪心。

### 离线步骤 5：选择 \(J_X,J_W\)

假设这一层总补偿预算为 \(S\)。固定 1:1 表示：

```text
J_X 选择激活分数最大的 S/2 个通道
J_W 选择权重分数最大的 S/2 个通道
```

例如 \(S=128\)：

```text
len(J_X) = 64
len(J_W) = 64
总补偿列数 = 128
```

当前实验中的总 \(S\) 沿用论文校准得到的每层 `select_num`，只改变补偿内容和选列方法。

### 离线步骤 6：准备补偿权重

激活残差分支需要原始权重的对应列：

\[
W[:,J_X]
\]

权重残差分支需要权重残差的对应列：

\[
E_W[:,J_W]
\]

把它们拼起来：

\[
R=
[W[:,J_X],E_W[:,J_W]]
\]

然后离线量化并保存：

\[
QR=\operatorname{Quant}(R)
\]

部署后不再重新计算 \(W-QW\)。

### 离线流程图

```text
量化 W
  ↓
得到 QW 和 E_W = W-QW
  ↓
用校准文本运行模型，获得每层 X
  ↓
量化 X，获得 QX 和 E_X = X-QX
  ↓
累计每个通道的激活/权重二阶统计量
  ↓
选择 J_X 和 J_W
  ↓
构造并量化补偿权重 QR
  ↓
保存 QR 和静态通道索引
```

---

## 7. V2 的在线流程

在线部分发生在真实 prefill 和 decode 中。

在线不会再执行：

- 校准统计。
- 通道打分。
- top-k。
- 权重残差计算。

### 在线步骤 1：执行普通 RTN

当前输入激活为 \(X\)，先执行普通激活量化：

\[
QX=\operatorname{Quant}(X)
\]

再执行主 GEMM：

\[
Y_{\text{main}}=QXQW^T
\]

这是普通 W4A4 RTN 本来就有的工作。

### 在线步骤 2：根据 \(J_X\) 生成当前激活残差

从当前请求的 \(X,QX\) 中取出 \(J_X\) 指定的位置：

\[
X[:,J_X]
\]

\[
QX[:,J_X]
\]

然后只对这些位置相减：

\[
E_X[:,J_X]
=
X[:,J_X]-QX[:,J_X]
\]

真实实现没有必要计算完整的 \(X-QX\)。

### 在线步骤 3：根据 \(J_W\) 取当前量化激活

权重残差分支只需要：

\[
QX[:,J_W]
\]

### 在线步骤 4：构造补偿激活

拼成宽度为 \(S\) 的左矩阵：

\[
L=
[E_X[:,J_X],QX[:,J_W]]
\]

然后量化：

\[
QL=\operatorname{Quant}(L)
\]

### 在线步骤 5：计算补偿并加回输出

补偿输出：

\[
Y_{\text{correction}}=QLQR^T
\]

最终输出：

\[
Y_{\text{final}}
=
Y_{\text{main}}+Y_{\text{correction}}
\]

也就是：

\[
Y_{\text{final}}
=
QXQW^T
+
Q([E_X[:,J_X],QX[:,J_W]])
Q([W[:,J_X],E_W[:,J_W]])^T
\]

### 在线流程图

```text
当前请求产生 X
  ↓
正常量化得到 QX
  ├──────────────→ 主 RTN：QX × QWᵀ
  │
  ├─按 J_X 取 X-QX
  └─按 J_W 取 QX
          ↓
拼成 S 列补偿激活并量化
          ↓
补偿 GEMM：QL × QRᵀ
          ↓
主 RTN 输出 + 补偿输出
```

---

## 8. V2 的小例子

假设输入只有 8 个通道：

```text
通道编号：0 1 2 3 4 5 6 7
```

总补偿预算：

```text
S = 4
```

固定 1:1，所以两边各选 2 个通道。

离线统计后得到：

```text
J_X = [1, 6]
J_W = [3, 6]
```

离线准备补偿权重：

\[
R=
[W[:,1],W[:,6],E_W[:,3],E_W[:,6]]
\]

在线对当前请求准备补偿激活：

\[
L=
[E_X[:,1],E_X[:,6],QX[:,3],QX[:,6]]
\]

补偿近似为：

\[
E_X[:,1]W[:,1]^T
+E_X[:,6]W[:,6]^T
+QX[:,3]E_W[:,3]^T
+QX[:,6]E_W[:,6]^T
\]

第 6 个通道出现两次，是因为它的激活误差和权重误差都重要。

---

## 9. V2 的伪代码

### 9.1 离线校准伪代码

```python
for each linear layer:
    QW = quantize(W)
    EW = W - QW

    activation_error_sum = zeros(K)
    quantized_activation_sum = zeros(K)

    for X in calibration_activations:
        QX = quantize(X)
        EX = X - QX

        activation_error_sum += sum_over_tokens(EX ** 2)
        quantized_activation_sum += sum_over_tokens(QX ** 2)

    activation_score = activation_error_sum * sum_over_outputs(W ** 2)
    weight_score = quantized_activation_sum * sum_over_outputs(EW ** 2)

    J_X = topk(activation_score, S // 2)
    J_W = topk(weight_score, S // 2)

    R = concat(W[:, J_X], EW[:, J_W], dim=input_channel)
    QR = quantize(R)

    save(J_X, J_W, QR)
```

### 9.2 在线推理伪代码

```python
QX = quantize(X)

main_output = gemm(QX, QW)

selected_activation_error = X[:, J_X] - QX[:, J_X]
selected_quantized_activation = QX[:, J_W]

L = concat(
    selected_activation_error,
    selected_quantized_activation,
    dim=input_channel,
)
QL = quantize(L)

correction = gemm(QL, QR)
output = main_output + correction
```

目标硬件实现应将主计算和补偿计算融合为一次增广 GEMM：

\[
[QX,QL]\,[QW,QR]^T
\]

---

## 10. V2 的开销记录

以下数据对应当前 Qwen2.5-1.5B-Instruct 实验。

### 10.1 离线开销

| 项目 | 当前估计 |
|---|---:|
| 激活统计 | 每个校准 token、每层 \(O(K)\) |
| 权重统计 | 每层一次 \(O(NK)\)，可随权重量化完成 |
| top-k | 每层两次，离线开销可忽略 |
| 全模型激活统计向量 | 约 3.9 MiB，可逐层释放 |
| 静态索引 | 约 49.8 KiB（`int16`） |

激活重要性统计是离线校准开销，不是每个 prompt 的在线开销。

### 10.2 在线持久存储

| 项目 | 当前估计 |
|---|---:|
| 主线性权重元素 | 1,310,195,712 |
| 补偿权重元素 | 81,264,640 |
| 补偿权重比例 | 6.20% |
| NVFP4 补偿权重（含每 16 值一个 FP8 scale 的估计） | 约 43.6 MiB |

### 10.3 在线计算

主 GEMM 的计算量：

\[
2MKN
\]

补偿 GEMM 的计算量：

\[
2MSN
\]

理论比例为：

\[
S/K
\]

按全模型实际层形状加权，额外 MAC 约为 6.20%。

在线新增激活处理元素数量约为普通逐模块激活宽度的 4.89%。

### 10.4 在线临时激活

全模型每个 token 累计产生 24,896 个补偿激活值。

按照 NVFP4 数据加 scale 估算：

```text
约 14.0 KiB / token
约 27.4 MiB / 2048-token prefill
```

这是全模型累计流量，不是峰值显存。buffer 可以逐层复用。

单模块最大 \(S=320\)：

```text
M=1 decode：约 180 B 的打包 NVFP4 补偿激活
M=2048 prefill：约 360 KiB 的打包 NVFP4 补偿激活
```

### 10.5 Prefill 和 Decode 预估

| 场景 | 融合实现的初步估计 | 主要风险 |
|---|---:|---|
| Prefill | 约 6%～10% | K+S tile 利用率、补偿激活写回 |
| Decode | 约 5%～10% | 权重带宽和 kernel launch |

以上不是实测值。

如果实现成“独立 gather + 独立补偿量化 + 第二个小 GEMM”，实际开销可能明显更高。目标实现必须尽量融合。

---

## 11. V2 的硬件落地思路

逐通道选择不一定需要在主量化结束后重新随机读取激活。

更合理的方式是在主 activation quantization 中顺手抽取：

```text
读取原始 X 的一个量化组
  ↓
计算该组的 QX 和 scale
  ↓
检查离线保存的 lane mask
  ↓
如果组内有 J_X：输出对应的 X-QX
如果组内有 J_W：输出对应的 QX
  ↓
连续写入 S-column 补偿 buffer
```

建议把逐通道索引预处理为：

```text
原始 group id
+ 16-bit lane mask
+ 补偿 buffer 写入位置
```

这样可以：

- 保持主 RTN 的原始 NVFP4 分组不变。
- 不改变 outlier 与普通值原本的分组关系。
- 不需要重新读取完整激活。
- 将散列通道连续打包成补偿 GEMM 的 \(S\) 列。
- 允许 Q/K/V 和 Gate/Up 在同一次输入扫描中提取各自需要的通道。

当前逐通道索引按共享输入取并集后，源通道准备量可以从约 22,112 降到约 15,683，减少约 29.1%，并不要求严格选择完整 16-channel block。

### 必须实测的三个实现版本

1. 独立 gather + 独立 correction GEMM，作为最差基线。
2. 融合主量化和逐通道抽取 + 独立 correction GEMM。
3. 融合逐通道抽取 + 单次 K+S GEMM，作为目标实现。

建议测试：

```text
M = 1, 16, 128, 2048
```

分别覆盖 decode、小 batch 和长 prefill。

---

## 12. V2 当前实验结果

### 12.1 五 seed 局部 output-SSE

| 方法 | Holdout output-SSE 改善 |
|---|---:|
| 论文 reorder + ARC | 21.44% ± 2.09% |
| 逐通道联合 K+S，固定 1:1 | 39.94% ± 1.40% |
| 逐通道联合 K+S，自适应 | 41.70% ± 1.37% |

### 12.2 完整 WikiText2 PPL

| 方法 | PPL，越低越好 |
|---|---:|
| BF16 | 9.6572 |
| 普通 RTN | 11.1541 |
| 论文 reorder + ARC | 10.7894 |
| 逐通道联合 K+S，固定 1:1 | 10.5937 |
| 逐通道联合 K+S，自适应 | 10.5855 |

固定 1:1 追回了普通 RTN 额外 PPL 损失的约 37.43%。

### 12.3 当前结论

- “联合补偿激活残差和权重残差”是有效的。
- 固定 1:1 和当前自适应分配的 PPL 基本打平。
- 逐通道选择的数学收益值得继续研究真实 kernel。
- 当前不能只根据 Python fake 后端耗时判断实际性能。

---

## 13. V2.1：逐通道联合 K+S，自适应分配

### 13.1 与 V2 的区别

V2 固定：

```text
激活分支 = S/2
权重分支 = S/2
```

V2.1 把两类候选分数放在一起比较，自动决定两个分支各占多少预算，再将分支宽度对齐到 16。

例如某个 `down_proj` 可能得到：

```text
激活分支约占 11%
权重分支约占 89%
```

### 13.2 当前定位

它说明不同模块确实具有不同误差分布，但完整 PPL 只比固定 1:1 改善 0.0082。

因此 V2.1 目前用于理解误差，不作为第一版硬件实现的优先方案。

---

## 14. V3：16-block 共享 K+S

### 14.1 动机

V3 强制以完整 16-channel block 为选择单位，并让：

- Q/K/V 共用索引。
- Gate/Up 共用索引。
- 可选地让激活和权重分支使用同一组 block。

这是为了估计最保守的连续访存约束会损失多少数值质量。

### 14.2 结果

| 方法 | 五 seed Holdout gain | 完整 PPL |
|---|---:|---:|
| 逐通道固定 1:1 | 39.94% ± 1.40% | 10.5937 |
| 16-block 共享双集合 | 27.57% ± 1.74% | 10.7922 |
| 16-block 共享同集合 | 27.66% ± 1.87% | 10.8030 |

### 14.3 当前结论

- 主要精度损失来自“逐通道变成整 16-block”。
- 已经按 block 选择后，共享 QKV/Gate-Up 和使用同一组 block 几乎不再损失精度。
- V3 的 PPL 回到论文 ARC 附近，没有保留 V2 的主要优势。
- 因此 V3 目前是硬件约束下界，不应直接替代 V2。

---

## 15. 算法版本之间的关系

```text
V0 普通 RTN
  │
  ├─ V1：只重点补激活残差，并使用论文重排
  │
  └─ V2：不改变主 RTN 分组，独立补激活残差和权重残差
       │
       ├─ V2.1：自动调整两个分支的预算
       │
       └─ V3：给 V2 加上严格 16-block 和共享索引约束
```

---

## 16. 代码和实验产物

| 内容 | 路径 |
|---|---|
| V2 选择和 holdout 实验 | `scripts/analyze_proxy_joint_ks_holdout.py` |
| V2/V3 完整 PPL | `scripts/evaluate_joint_ks_ppl.py` |
| V3 选择和硬件代理实验 | `scripts/analyze_hardware_aware_ks_holdout.py` |
| 五 seed V2 汇总 | `analysis/rtn_proxy_ks_multiseed/` |
| 完整 V2 PPL | `analysis/rtn_joint_ks_ppl/` |
| V3 汇总 | `analysis/rtn_hwaware_ks_final/` |
| 完整分析报告 | `analysis/rtn_reorder_arc/report.html` |

---

## 17. 新算法版本模板

以后增加算法时，复制下面的结构继续追加。

```markdown
## VX：算法名称

### X.1 一句话说明

这个版本想解决什么问题，做了什么核心改变。

### X.2 动机

- 之前版本的问题是什么？
- 新观察是什么？
- 为什么值得增加这个版本？

### X.3 与已有版本的区别

| 对比项 | 旧版本 | 新版本 |
|---|---|---|
| 选列方法 | | |
| 激活分支 | | |
| 权重分支 | | |
| 是否重排 | | |
| 是否需要在线选择 | | |
| 硬件布局 | | |

### X.4 离线流程

1. ...
2. ...
3. ...

### X.5 在线流程

1. ...
2. ...
3. ...

### X.6 公式或伪代码

写清输入、输出、索引和补偿内容。

### X.7 计算和存储开销

| 项目 | 开销 | 是否实测 |
|---|---:|---|
| 校准 | | |
| 持久权重 | | |
| 索引 | | |
| 在线激活 | | |
| GEMM | | |

### X.8 实验设置

- 模型：
- 数据集：
- 校准样本：
- Holdout：
- 量化后端：
- Rotation：
- Seed：

### X.9 实验结果

记录局部 output-SSE、完整 PPL、任务准确率和硬件性能。

### X.10 当前结论和下一步

- 已验证：
- 未验证：
- 已知限制：
- 下一步：
```

---

## 18. 文档更新日志

| 日期 | 变更 |
|---|---|
| 2026-08-20 | 创建算法设计文档；整理 V0、V1、V2、V2.1、V3；重点补充 V2 的大白话说明、离线/在线流程、开销和伪代码。 |
| 2026-08-20 | 增加 V2 双索引必要性消融：相同总预算下比较独立、共享、单分支、随机和 output-aware 诊断；五 seed 局部 SSE 与五 calibration seed PPL 均支持独立 \(J_X/J_W\)，但优势小而稳定，不能单独支撑整篇论文。完整结果见 `analysis/rtn_dual_index_ablation/REPORT.md`。 |
